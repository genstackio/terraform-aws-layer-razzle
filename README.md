# AWS Razzle Terraform module

Terraform module which creates the resources to host a razzle web app on AWS.

## Usage

```hcl
module "main" {
  source      = "genstackio/layer-razzle/aws"

  // ...
}
```
