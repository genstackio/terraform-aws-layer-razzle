# AWS lambda-origin-response Terraform module

Terraform module which create lambda on AWS.

## Usage

```hcl
module "lambda-origin-response" {
  source = "genstackio/layer-razzle/aws//modules/lambda-origin-response"

  // ...
}
```
